# Agent Instructions

If you are an AI coding agent and this file was auto-discovered: read
[`SKILL.md`](SKILL.md) before doing anything else in this repository, or
before applying this skill to another project. `SKILL.md` is the entry
point; it links one level deep into `reference/` and `prompts/` for
everything else.

If you were asked to playtest an app that lives elsewhere, copy or reference
this repository's `SKILL.md`, `reference/`, `prompts/`, and
`scripts/validate_evidence.py` — they are self-contained and do not depend on
anything specific to this repository.
