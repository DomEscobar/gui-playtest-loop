# App Guide

Filled in by the Builder at the end of the first build, before the first
playtest round. This is the only thing besides `goal.json` that the
playtester reads before phase 1.

## Run

- Start command: `<command>`
- URL: `<url>`
- Any required env vars or seed data: `<...>`

## Known assumptions

List anything the builder assumed that is not obvious from the goal, e.g.
"assumes a logged-out session," "assumes a fresh database," "layout targets
desktop width only."

## Expected behaviors

Copy the human-readable form of `goal.json`'s checks here so a human skimming
the run folder does not need to open JSON to understand intent.

1. <check statement>
2. <check statement>
3. ...

## Out of scope for this round

Anything explicitly deferred, so the playtester does not report it as a
failure of the current goal.
